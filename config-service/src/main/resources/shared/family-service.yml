security:
  oauth2:
    client:
      clientId: server
      clientSecret: secret
      accessTokenUri: ${AUTH_SERVICE:http://localhost:8000}/auth/oauth/token
      grant-type: client_credentials
      scope: server