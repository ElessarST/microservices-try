security:
  oauth2:
    client:
      clientId: server
      clientSecret: secret
      accessTokenUri: ${AUTH_SERVICE_URL:http://localhost:8000}/auth/oauth/token
      grant-type: client_credentials
      scope: server