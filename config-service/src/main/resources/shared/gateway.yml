security:
    oauth2:
        client:
            clientId: app
            clientSecret: secret
            accessTokenUri: ${AUTH_SERVICE_URL:http://localhost:8000}/auth/oauth/token
            clientAuthenticationScheme: form
            scope:
                - webclient